# Traspaso — 2026-09-13

Sesión 1: limpieza final de residuos de prueba (Sprint 6, workflow STAGING) + sincronización del repo (Sección J.4 de `STATUS.md`).
Sesión 2 (mismo día): transición de STAGING a producción (Sección J.5 de `STATUS.md`).
Sesión 3 (mismo día): persistencia de `epr`/`tiempo_eyectivo` + alerta de fallo del eco (gap H7) + limpieza de índice redundante (Sección J.6 de `STATUS.md`) — ver bloque siguiente.

Este archivo es un resumen rápido para retomar; el detalle completo vive en `STATUS.md`.

## ⚡ Estado tras persistencia eco + alerta + limpieza de índice (Sesión 3, 2026-09-13)

- **`epr` y `tiempo_eyectivo` se persisten** en `datos_ecocardiografia` (columnas nuevas, migración `add_epr_tiempo_eyectivo_to_datos_ecocardiografia`) — ya se calculaban en el SPA, solo faltaba sumarlos a `COLUMNAS_DATOS_ECO`. Verificado E2E con valores reales en Supabase.
- **Gap H7 cerrado:** 3 nodos nuevos en `lkOwTFmVTZu7EMoU` (`IF - ¿Persistió Eco?` → `Preparar alerta eco` → `Alertar fallo eco`, Gmail a `echevanest@gmail.com`+`infoacivet@gmail.com`) — un fallo del insert de eco ahora alerta solo, sin depender de que falle también la Atención (keystone). Verificado con una prueba forzada aislada: se disparó únicamente la alerta nueva, no la vieja.
- **`tutores.email`:** se mantiene el `UNIQUE` plano (`tutores_email_unique`) — sostiene el fallback `on_conflict=email` del `Upsert Tutor` para cuando el bookmarklet no puede extraer `idTutor` (caso real, no hipotético — ver `bookmarklet/launcher.js`, `extraerIdTutor()`). Se eliminó, sí, el índice parcial redundante `tutores_email_unq` (sin dependencias, ya marcado como deuda en `schema.sql`).
- **Residuos de las pruebas E2E:** Supabase limpio (verificado). **Drive/Sheets sigue bloqueado** para esta sesión de Claude (mismo motivo que la limpieza de J.4) — pendiente que Marcelo borre a mano 3 PDFs (`ZZTESTEPRLOLA...`, `ZZTESTEPRFALLOLOLA...`, `ZZTESTEPRFALLO2LOLA...`) y sus 3 filas en la planilla Índice.
- Commits locales (sin push): persistencia epr/tiempo_eyectivo + remoción de debug log, alerta de fallo del eco, eliminación del índice redundante, esta documentación.

## ⚡ Estado post-transición a producción (Sesión 2, 2026-09-13)

**Publicado.** Ya no aplica "ningún workflow está publicado" de más abajo (queda como registro histórico de la Sesión 1).

| Workflow | Rol | `active` | Path webhook |
|---|---|---|---|
| CORE (`5gGWXOjY2BBOAfuw`) | **Backup** — `"MYVETE - Ingesta (CORE) [BACKUP - NO TOCAR]"` | `false` | `ingesta-filiacion-v4` (intacto) |
| STAGING/B (`lkOwTFmVTZu7EMoU`) | **Producción** | `true` | `ingesta-filiacion` (antes `ingesta-filiacion-v6-test`) |

- SPA (`interface/app.js:683`) apunta a `.../webhook/ingesta-filiacion` — actualizado en el mismo turno que la publicación.
- CORE **no se borra** hasta validar 3-7 días de tráfico real en producción (roadmap 2.4) — decisión pendiente, con Marcelo.
- No se disparó tráfico real en esta sesión; la validación en producción (roadmap 2.2) es tarea aparte con autorización explícita.
- Hallazgo técnico para futuras automatizaciones: `active` es **read-only** en `PUT /workflows/{id}` de esta API de n8n — usar `POST /workflows/{id}/activate` / `.../deactivate`. El `PUT` solo acepta `name`, `nodes`, `connections`, `settings`, `staticData` en el body (cualquier otro campo del GET completo devuelve `400`).
- Commits locales de esta sesión (sin push a `origin`): publicar STAGING como producción, marcar CORE como backup, actualizar webhook del SPA, esta documentación + `STATUS.md` Sección J.5.
- Rollbacks guardados: `n8n/workflow_v6_pdf_mail.STAGING.pre-prod.json` (STAGING antes de publicar).

---

## Resumen de la Sesión 1 (limpieza de residuos, previo a la transición)

## Estado de Fixes (workflow `lkOwTFmVTZu7EMoU`, 26 nodos, `active: false`)

| Fix | Estado |
|---|---|
| 1 — credencial Drive ARES→infoacivet | Completo (2026-09-12) |
| 2 — esquema `Registrar en Índice` | Completo (2026-09-13) |
| 3 — reconectar Gmail INFOACIVET | **No verificable por API** — acción de Marcelo en la UI de n8n |
| 4 — limpieza residuos Prueba A | Absorbido por la limpieza final de esta sesión |
| 5 — normalización Ñ en nombre de PDF | Completo (2026-09-13) |
| 6 — fix nombre_pdf con `$items()` | **Insuficiente**, reemplazado por Fix 7 |
| 7 — fix nombre_pdf con nodos Set | Completo (2026-09-13), solución de raíz |
| 8 — validación de campos obligatorios | No iniciado |

## Estado de Pruebas E2E en STAGING

- **Prueba A** (flujo limpio): ✅ OK
- **Prueba B1** (upsert-update + colisión de nombre): ✅ OK
- **Prueba B2** (fallo forzado de persistencia + alerta): ✅ OK

Las 3 corridas contra el webhook `ingesta-filiacion-v6-test`, no contra producción.

## Residuos — IDs (ya borrados, se dejan para historia)

**Supabase (`tuedigqvvkvgongpcnjx`) — borrados y verificados en 0:**
- `tutores.id = a7ef1ef0-c2ec-4816-9baa-16d80886b117` (MUÑOZ, PEDRO / `id_myvete=ZZ_TEST_E2E_SPRINT6_PRUEBA_A`)
- `mascotas.id = 40b8ca97-474f-4992-aa1d-85258a6bb8c2` (ZZ_TEST_LOLA)
- `atenciones_cardiologia.id = a259e8e5-544d-4215-934b-b55a603941d3` (Prueba A)
- `atenciones_cardiologia.id = 0217b3b5-4aeb-4cea-964f-e540b3bd0c5d` (Prueba B1)
- `datos_ecocardiografia` de ambas atenciones (cascada FK)

**Drive/Sheets — PENDIENTE, no se pudo tocar desde esta sesión (sin acceso a la cuenta de Google real):**
- Carpeta `1_lcbkiJK5ql_1sCNB3kkhDOES7Xgv7Vn`: PDFs `ZZTESTLOLAMUÑOZ SEPTIEMBRE 2026.pdf` (`1vpxJneXsqdpwfnE-P5XTrtQhvaI5HdRK`) y `ZZTESTLOLAMUÑOZ (PEDRO) SEPTIEMBRE 2026.pdf` (`1rVgqAylBn2hFEaoXpe2_bS7ufP8OceDY`) — buscar además cualquier otro `ZZTEST*` (la Prueba B2 pudo haber generado uno).
- Planilla `1Zxn38DFlpGBlIKSsh_94ZfdnWHC5D3j9y9eiOTGwCxU`, `Hoja 1`: filas con paciente `ZZ_TEST_LOLA` (al menos 2 conocidas, revisar si la Prueba B2 agregó una con `estado_persistencia=FALLO`).

## Estado de git al cierre de esta sesión

- Rama `master`, sin push a `origin` (esperando autorización explícita, como en sesiones previas).
- Commits locales de esta sesión: re-export del workflow + `STATUS.md` + este archivo (ver mensaje del commit).
- `interface/app.js` / `interface/index.html`: cambios preexistentes de la Sección I (UI eco/EKG), revisados pero **no commiteados** en esta sesión — decisión de Marcelo.
- `supabase/schema.sql`: cambio preexistente, solo un comentario ampliado (sin DDL), **no commiteado** — decisión de Marcelo.

## Pendientes de Fase 2 (sin cambios de prioridad)

1. Fix 8 — validación de campos obligatorios.
2. Sección ECG en el PDF (bloque comentado en `Preparar Datos para PDF`).
3. SPA de búsqueda.
4. Warning en la respuesta del webhook cuando falla la persistencia.
5. Botón "Abrir en pestaña" para el PDF/Doc.
6. Persistencia de medicación + EKG.
7. Confirmar Fix 3 (Gmail INFOACIVET) en la UI de n8n.
8. Publicar workflow B como producción → validar 3-7 días en real → borrar workflow CORE (`5gGWXOjY2BBOAfuw`).

## Lecciones de la sesión 2026-09-13

- **Patrón de bug n8n (Fix 6→7):** una expresión n8n que referencia un nodo (`$('Nodo')`) lanza excepción si ese nodo no ejecutó — *antes* de que cualquier operador que la envuelva (`??`, `?.`, `$items()`) pueda amortiguarla. La única solución robusta cuando el valor depende de una rama condicional aguas arriba es un nodo Set/Edit Fields que unifique el resultado en cada rama, no una expresión "más defensiva" en el nodo consumidor.
- **Verificar acceso a servicios externos (Drive/Sheets) al principio de una tarea de limpieza, no al final** — habría evitado planificar pasos que terminaron bloqueados por permisos de la cuenta conectada a esta sesión.
